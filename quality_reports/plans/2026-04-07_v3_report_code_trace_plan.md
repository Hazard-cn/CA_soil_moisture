# 2026-04-07 v3 报告实证结果代码追踪计划

## 任务
根据 `scripts/`、`output/logs/` 与 `output/reports/v3_beamer_interaction_report.tex`，识别该报告中各项实证结果对应的代码文件，并整理为可核对清单。

## 步骤
1. 读取 `v3_beamer_interaction_report.tex`，提取表格、图片、输入文件与结果描述。
2. 对照 `output/logs/` 中 v3 及相关历史日志，确认实际执行过的脚本名称。
3. 在 `scripts/` 中检索结果文件名、导出语句与标题文本，建立“报告结果 -> 脚本文件”映射。
4. 输出高置信度清单，并单独标注间接生成或证据不足项。

## 验证
- 至少同时满足以下两类证据中的两类：报告引用路径、日志执行记录、脚本中的导出/写出语句。
