# 37篇相关文献GitHub发布计划

日期：2026-07-19。目标仓库：`Hazard-cn/CA_soil_moisture`。目标分支：`work/literature-37`。

## 发布范围

发布严格限定于 Zotero collection `CA_mechanism_reclassified_2026-06-18` 的37篇文献，包含书目信息、DOI、官方论文链接、相关性与可学习点、全文及补充材料本地核验状态、公开代码来源与本地映射，以及许可证明确允许再分发的纯文本代码。论文PDF、补充材料全文、数据集、模型、缓存、日志、图片、二进制分析产物、许可证缺失代码和访问受限材料不得进入Git历史。

## 分支与隔离

当前主工作区包含用户的无关修改，且本地 `main` 落后 `origin/main`。因此从更新后的 `origin/main` 建立独立worktree和 `work/literature-37` 分支，不切换、不暂存、不修改当前工作区的既有变更。所有提交只显式暂存本计划和本任务新建的发布目录。

## 执行步骤

1. 复核37篇统一清单、学习要点、全文附件状态、代码来源和许可证证据。
2. 从 `origin/main` 创建隔离工作分支，复用已发布的 `docs/results/report-sr-method-portfolio-web-reader-v1/literature/` 规范入口，并补充许可代码链接和发布边界。
3. 只复制许可证明确允许再分发的源代码，并逐文件记录原始来源、许可证、固定版本、SHA-256和论文映射；其余代码仅保留官方链接。
4. 检查Git对象中不存在PDF、数据、归档、图片、模型、日志或超限文件，运行仓库策略检查、语法检查、链接与37条范围校验。
5. 显式暂存本任务文件，提交、推送 `work/literature-37`，创建以 `main` 为目标的PR；通过必要检查后按项目默认授权合并。

## 验收标准

- 37条记录完整且rank、DOI、Zotero父条目key唯一。
- 每篇至少具有官方论文链接和代码状态；有学习要点的原始排序被保留。
- 所有被复制代码均有明确可再分发许可证及归属说明，且没有执行第三方代码。
- Git提交不包含论文PDF、数据、压缩包、图片、模型、缓存、日志和无许可证代码。
- `python .github/scripts/check_repository_policy.py --source index`、语法检查、Git范围检查和PR检查通过。

## 执行记录

- `origin/main` 已有37篇排序清单：37个rank、37个DOI链接、111个逐篇学习点；本次不重复建立书目目录。
- 许可证复核后公开8篇的137个纯文本代码文件和4个必要许可证/包元数据文件；另有71个上游文件因机器绝对路径、Python语法或发布类型边界而未进入Git。
- 为避免Windows `core.autocrlf` 改变第三方代码字节，发布目录在 `.gitattributes` 中设为 `-text`；141/141个清单条目的暂存blob字节数与SHA-256一致。
- 仓库策略检查通过：733个暂存文件、210个Python文件，0错误；244个既有机器路径仅产生基线警告，本次未新增。
- 谱系严格验证、文档一致性检查和8个谱系单元测试通过；仓库及本次发布共54个R文件解析通过。第三方代码未执行。
- Windows pre-push按完整提交SHA读取长路径时，`git show`触发路径长度错误；策略脚本改用`git cat-file blob`直接读取Git对象，并须对index、HEAD和完整提交SHA分别复核。
