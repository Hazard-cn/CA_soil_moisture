# G185旧方法统一override自包含HTML最终独立审查

## 最终判定

**判定：`PASS_SELFCONTAINED_HTML_FINAL`，92/100。** 本轮没有发现Critical、Major或阻断性Minor。公共稿上一轮唯一遗留的远端图表持久化Minor已经关闭：三幅受审PNG均以base64直接嵌入HTML，正文、表格、图注、公式文本、DOI和核心数值均随单个HTML文件呈现；页面不依赖外部CSS、JavaScript、字体或图片文件。该判定只确认锁定Markdown到自包含HTML的内容同一性、资源完整性、确定性和浏览器呈现，不扩张原公共稿的实证主张，也不把92分候选稿改称95分投稿完成稿。

审查对象为：

- `docs/results/g185-old-method-unified-override-v1/report.html`
- `docs/results/g185-old-method-unified-override-v1/report.md`
- `scripts/python/render_g185_override_selfcontained_html.py`
- `tests/test_render_g185_override_selfcontained_html.py`
- `quality_reports/plans/2026-07-15_g185_override_selfcontained_html_check.md`
- `quality_reports/plans/2026-07-15_g185_override_public_final_review.md`

本轮没有修改HTML、Markdown、PNG、渲染器、测试、机器表或历史审查记录；唯一新增文件为本审查报告。

## 文件身份与确定性重渲染

锁定Markdown的实算SHA-256为 `8767cf008d67550d9708e313f529dcc6cc1042768413300e8f8037ae9afd5e1a`，与渲染器内置的来源锁一致。HTML实算大小为748,315 bytes，SHA-256为 `9d5d9fc0c795d46712ca94320796e59fe62b467a76606745248a71c0a212a1e5`。

本轮没有直接采信自检文件的登记值，而是独立导入渲染器，在内存中对同一锁定Markdown连续渲染两次。当前HTML、第一次重渲染和第二次重渲染三者字节完全相同，三次SHA-256均为 `9d5d9fc0c795d46712ca94320796e59fe62b467a76606745248a71c0a212a1e5`。因此，当前制品可由锁定来源确定性复现，未包含时间戳、随机标识或环境相关的绝对路径。

## 三幅内嵌图像逐一复核

本轮按Markdown出现顺序独立解析三个 `data:image/png;base64` 资源，对base64使用严格解码，并分别与Markdown原始相对路径解析到的受审PNG计算SHA-256。结果如下：

| 图像 | 受审PNG SHA-256 | HTML解码SHA-256 | 结果 |
|---|---|---|---|
| `fig1_national_iede.png` | `3a631fae43790d4bbabec599b773fcc31788ff1e46db4d89bf8af7a7962539f7` | `3a631fae43790d4bbabec599b773fcc31788ff1e46db4d89bf8af7a7962539f7` | 一致 |
| `fig2_core_linear_curves.png` | `ce7457b31ec391ba058649d4e53318480da2494792c9eb1b5b36c740d920eeb9` | `ce7457b31ec391ba058649d4e53318480da2494792c9eb1b5b36c740d920eeb9` | 一致 |
| `fig3_five_zone_iede.png` | `82aa88eaa7a34fa100dc5a25ec7b8f770407e58770b2e53b8bea86f0192143d9` | `82aa88eaa7a34fa100dc5a25ec7b8f770407e58770b2e53b8bea86f0192143d9` | 一致 |

三个alt文本也按顺序对应全国组件图、三个核心区域旧线性曲线和五区分解图，没有错位。三幅图在真实浏览器中均完成解码，天然尺寸均为3600×1500像素，在1440像素宽视口内按1120像素宽等比例显示，没有裁切或拉伸。

## Markdown内容、表格、DOI与核心数字覆盖

独立HTML解析得到1个 `article`、1个一级标题、11个二级标题、1个三级标题、44个段落、6张表、40个表格行、4个公式块、3个figure和3个image，与自检登记结构一致。测试逐行去除Markdown标记并与HTML可见文本比对，所有非空内容行均可见，缺失项为0；因此摘要、方法、五区结果、土壤水分边界、稳健性、局限、投稿匹配、声明和参考文献没有在渲染过程中丢失。

HTML可见文本包含锁定测试指定的全国端点 `0.5768`、`1.1580`、`1.1464`，空间扰动比例 `84.1921%`，以及 `1,683次` 和 `1,999次` 等核心数字。Markdown中的5个DOI全部保留：`10.1037/a0020761`、`10.1038/s43016-021-00341-6`、`10.1038/s41467-020-18631-1`、`10.1038/s43016-022-00592-x` 和 `10.1097/EDE.0000000000000596`。本轮未重新判定这些文献的学术适用性；该部分沿用已经通过三层来源核验和公共稿最终审查的锁定引用集合，只核验HTML没有改变或遗漏。

## 自包含资源与路径安全

HTML仅有1个内联 `style` 元素，`link` 元素为0，`script` 元素为0；除三个base64 PNG外不存在其他 `src`。全文没有 `file://`、Windows盘符绝对路径、UNC路径、外部CSS或外部JavaScript。5个DOI、Scientific Reports范围页以及项目内代码和机器制品仍以普通超链接保留，它们不参与页面正文、样式、表格或图像渲染。

其中4个指向 `temp/` 的相对补充链接均附有可见的“本地制品”标签。这些链接在脱离项目目录后不能点击到机器补充件，但不构成HTML显示依赖；单文件离线打开仍可完整阅读正文、六张表和三幅图。因而本轮关闭的是“报告及图表自包含”问题，不把HTML表述为已经嵌入全部CSV、manifest或模型代码的归档包。

## 真实浏览器版式核验

本轮通过本机HTTP服务在Playwright实际浏览器中打开HTML，而不是只进行字符串或DOM静态检查。核验视口为1440×1000像素，页面总高度8646像素；文档 `scrollWidth` 与 `clientWidth` 均为1440像素，没有页面级横向溢出。六张表的实际宽度均为1118像素，父容器宽度均为1120像素，六表均无内部溢出。顶部元数据、摘要、正文标题、图1、图3、完整15项检验表和全页长截图均经实际查看，中文字符、英文缩写、数字、表头、图注和参考文献可辨认，图表没有重叠、截断或超出正文栏。

浏览器控制台为0条warning、1条error；唯一error是浏览器惯例性请求未声明的 `favicon.ico` 返回404。HTML本身没有favicon引用，该请求不影响正文或资源完整性，也没有发现任何受审资源加载失败，因此列为非阻断性排版提示，不计为稿件Minor。若后续需要零错误控制台，可在不引入外部文件的前提下增加空data favicon，但该修改并非当前发布门槛。

## 可执行测试

在禁用Python字节码写入的条件下运行：

`python -m unittest tests.test_render_g185_override_selfcontained_html -v`

六项测试全部通过，覆盖来源身份与确定性输出、独立资源与路径安全、三幅PNG哈希、结构与核心数值和DOI、Markdown逐行可见性、以及本地补充链接标签。渲染器和测试文件另经Python AST解析，二者均通过语法检查。该过程没有在仓库内新增 `__pycache__` 或改写任何受审文件。

## 对上一轮公共持久化Minor的处理

上一轮 `PASS_PUBLIC_FINAL` 的唯一Minor是三幅图和部分补充机器链接位于不进入Git的 `temp/`，使远端Markdown本身不能显示图像。本轮HTML已经把三幅受审PNG按原哈希嵌入文件，公共正文、可见图注和图像现在共同位于 `docs/results/g185-old-method-unified-override-v1/report.html`。因此，图表持久化Minor判定为 **CLOSED**。

相对CSV、manifest和代码链接继续作为明示的项目内复核入口，不是HTML资源依赖；这与项目规则中机器数据、中间面板、日志和二进制分析输出不纳入Git的边界一致。若未来要求形成外部匿名复现包，应另行生成受控机器补充归档并登记新哈希，不能把当前单文件HTML等同于完整数据复现包。

## 固定六维评分

| 维度 | 得分 | 最终审查依据 |
|---|---:|---|
| 创新与期刊匹配 | 16/20 | 自包含HTML不改变既有G185统一组织的创新边界。 |
| 数据和五区支持 | 19/20 | 锁定正文和六表完整保留；SH低空间块限制未被排版掩盖。 |
| estimand与识别边界 | 20/20 | 两方程代数组件、IQR差、百分比端点和单位SR检验量均原样保留。 |
| SM构念及时序 | 15/15 | 同期SM定义、单位、窗口和非因果限制完整呈现。 |
| 精度与稳定性 | 12/15 | 既有NE 84.1921%、SH 13块和多数组合跨零等限制不因HTML发布而改变。 |
| 可复现性和图表完整性 | 10/10 | 来源锁、确定性重渲染、base64图像同哈希、内联样式、浏览器实测和自动测试全部通过。 |
| **合计** | **92/100** | 无Critical、无Major、无阻断性Minor；不按显著性加分，仍未达到95分投稿完成门槛。 |

## 最终结论

`report.html` 通过自包含HTML最终独立审查，判定 `PASS_SELFCONTAINED_HTML_FINAL`。文件可由锁定Markdown确定性重建，三幅图与受审PNG逐字节同源，全部Markdown核心内容、六张表和五个DOI均保留，页面没有绝对本地路径或外部样式和脚本依赖，实际浏览器版式没有横向溢出、表格裁切或图像加载失败。后续若只分发该HTML，可称为“正文与图表自包含候选稿”；不得据此称为已嵌入全部机器补充件的匿名复现包，也不得把92分状态提升为95分投稿完成。
