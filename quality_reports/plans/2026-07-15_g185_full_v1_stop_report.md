# G185 旧方法统一稿 full_v1 STOP 报告

## 状态

- Run ID：`2026-07-14_g185_old_method_unified_full_v1`
- 判定：`STOP`
- 解释边界：完整模型计算的投稿推进门槛未通过；不得进入 `docs/results`，不得写投稿结论，不得通过其他规格或新run补位。
- 失败目录：`temp/2026-07-14_g185_old_method_unified_full_v1/`

## 唯一触发的STOP规则

三个预设核心组合在 `grid FE + province×year FE` 下的点估计均保持正号，但NE干旱的2°空间块Rademacher非线性端点draw同向比例为 `0.8419209605`，低于冻结门槛 `0.90`。其province×year端点为 `0.8415784270%`，历史grid+year端点为 `1.8502198111%`。

HHH高温的province×year端点为 `2.1996347138%`，同向比例为 `0.9864932466`；HHH热干端点为 `1.6215617026%`，同向比例为 `0.9174587294`。二者没有触发该门槛。

该STOP是预设稳定性门槛的机械结果，不表示NE干旱点估计发生符号反转，也不得通过降低同向比例门槛、改变固定效应、改变空间块或筛选其他结果解除。

## 已通过的机械核验

- 全国历史P75−P25 TE三值最大误差：`2.6893413e-07`个百分点。
- 三个修正核心TE点估计及旧seed区间最大复现误差：`9.4391162e-12`。
- `TE_delta=IE_delta+DE_delta` 最大误差：`3.4694470e-18`。
- 三条核心曲线P90端点误差：`0`。
- 100/200/300 km两方程联合Conley HAC：`108/108`项完成且有限。
- 旧 `1.50%`、`3.27%`、`2.56%` 在实现中仅建立为 `DE` 参考标签。

## 失败产物与哈希

失败run在STOP后未继续落盘估计表、图表或标准run manifest，仅保留以下两个文件：

- `FULL_STOP.json`：SHA-256 `13de582e903adf782ba977a6dff22f2de15b4590d579b109469618841c580871`。
- `FULL_FAILED.json`：SHA-256 `436f549f8748bdc955dfd684f457dfcb530b32af1393c478a771d235ad8750f5`。

该行为保留了STOP证据并避免在门槛失败后写入可被误用为通过结果的表图；同时意味着本run不具备完整结果包和标准run manifest。后续若需要完整失败结果归档，必须先由独立结果审查明确授权新的失败归档设计，不能重新打开或补写本run，也不能自行启动新run。

## 代码验证

13项单元/集成测试、Python语法检查、JSON Schema Draft 2020-12 schema自检和 `git diff --check` 均通过。完整运行未提交Git；执行时的Git提交只能视为基线，当前新增执行脚本和core修改仍由工作树文件内容确定。
