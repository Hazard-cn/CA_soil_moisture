# G185 旧方法区域 IE/DE/TE 重绘收尾计划

日期：2026-07-10

## 目标

将 2026-06-24 完成的 G185 旧方法区域 IE/DE/TE 重估与五图重绘结果，同步到项目中面向后续使用者、后续 Agent 与跨会话记忆的正确位置，并验证所有长期记录与真实脚本、产物一致。

## 范围

1. 盘点项目根 Markdown、`docs/`、交接文件、项目记忆和 Codex 持久记忆。
2. 核对修正版脚本、输出目录、ZIP、README、断言和核心数值。
3. 创建缺失的项目入口 README，并更新 G185 方法升级交接与项目级记忆。
4. 记录稳定的跨会话纠错事实：旧图中的 1.50%、3.27%、2.56% 是控制同期土壤水分后的 DE / residual component，不是 IE+DE 定义下的 TE。
5. 检查路径存在性、文档链接、术语边界、相对时间和重跑命令。

## 约束

- 不删除任何文件、目录、输出、日志、计划或中间产物。
- 不把 G057/G049 结果混入 G185 证据。
- IE/DE/TE 仅描述两方程形成的代数组件，不表述为因果中介效应。
- 代码与数值以实际产物和脚本为准，文档不复制完整分析结果表。

## 验收

- 新使用者可从项目根 README 定位当前 G185 入口、重跑命令、修正版图表和证据边界。
- 交接文档明确区分旧 residual-only 数值与修正后的区域 TE。
- 项目记忆与 Codex 持久记忆更新说明无重复、无相对时间、无失效路径。
- 所有列出的关键路径存在，ZIP 完整，断言为 PASS，重绘脚本通过语法检查。
