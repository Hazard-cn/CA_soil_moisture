# SR方法组合继续执行计划（用户授权放宽STOP门槛）

## 状态与边界

- 用户于2026-07-15明确要求“继续执行，不考虑这个门槛”。本轮据此取消三个既有STOP条件的阻断效力，但不删除、覆盖或改写`regional-threshold-sr-v1`、`g185-old-method-unified-v1`和`compound-event-intensity-duration-v1`的历史审查结论。
- 新运行分别使用`regional-threshold-sr-override-v1`、`g185-old-method-unified-override-v1`和`compound-event-intensity-duration-override-v1`。结果必须显式披露原门槛及实际支持，不得把用户授权放宽写成方法学通过。
- 原始主工作目录保持不动；本轮在`work/sr-method-portfolio-override`隔离worktree执行。运行数据、日志、模型和二进制图表进入新的`temp/<run-id>/`，审核通过的Markdown进入`docs/results/<canonical-id>/`。
- “取消门槛”只取消停止运行，不取消数据哈希、算法单元测试、样本唯一性、模型可计算性、结果完整性和独立审核。禁止按显著性替换模型或删减五区结果。

## 执行方向

### 地区异质温度阈值

在外生阈值覆盖的complete-case样本上继续，西南地区保留69.84%的实际覆盖并单独报告。主暴露定义为逐日Tmax超过连续地区阈值的degree-day或exceedance-day指标；由于参考`CalExposure.m`使用逐小时温度，本轮日尺度实现必须命名为daily approximation，不能声称精确移植。完整生育季为主窗口，V3—HE和HE—MA为次级窗口；固定32°C作为预设对照。先执行数据与暴露构建，再运行五区完全交互模型、LOYO稳定性和SM状态/通道结果。

### G185旧方法统一稿

直接承接已完成的full package，不再以东北干旱空间块同向比例84.19%作为停止条件。重新核验机器结果后生成全国、连续区域缓冲曲线、五区IE/DE/TE分解、空间推断及完整表格；正文必须同时披露历史year FE与新增province×year FE结果，并把东北干旱空间稳定性不足列为核心局限。

### 热干事件强度—持续时间

先补齐runner测试文件到新的override manifest，重新执行smoke并核验身份闭包；随后运行全量Stage 1支持构建。即使个别地区事件数、右删失率或年份集中度不满足原门槛，也继续估计，但完整披露支持分母和不确定性。只有输入缺失、时间轴错误、事件定义实现错误或模型不可识别等计算性问题仍可阻断。全量支持完成后运行duration/intensity产量模型、SM drawdown和30日recovery分析。

## 审核与阶段

1. 数据/实现代理分别负责三个方向，先复核输入、命令、输出位置和历史run复用边界。
2. 每个方向的实现与结果由不同代理审核；原数值门槛只作为警示项，不再形成STOP。
3. 三个方向形成中文小稿，至少包含样本支持表、主结果表、五区异质性表和三幅图；没有可计算结果的方向必须交付明确的计算失败报告。
4. 最终统一执行仓库政策、谱系、测试、公开报告哈希和独立完整性审核，再通过PR进入`main`。

## 当前阶段

当前进入实验执行桥接阶段：先完成三个方向的输入/代码复核与override设计冻结，再运行模型。用户本条“继续执行”视为对本阶段计算执行的明确授权；新的论文完整性与审稿阶段仍按Academic Research Suite设置独立检查点。

## Pipeline预算记录

- 用户已授权multi-agent无限制使用，本轮不设显式token上限；运行计算预算限定为三个预注册方向及其smoke/full/复核，不扩展到新的规格搜索。
- 当前累计pipeline round-trip为1次override授权。实验结果形成后设置一次FULL检查点；稿件完整性、首次审稿、修订后复审和最终完整性属于后续强制检查点。
- 写作与返修仍遵守最多两轮修订；完整性失败最多三轮修复—复核。超出这些上限时必须显式报告，而不是继续循环。
