# Zotero 写入与复核记录（公开摘要）

本文件只发布书目写入的计数审计，不公开 collection key、item key、selected target ID 或本地接口细节。

## 2026-07-14 历史快照

写入前，目标 collection 有146个顶层条目，其中12篇已经存在。经来源存在性、DOI—题名和去重检查后，新增25篇文献和1个37篇综合排序索引，写入后的历史快照为172个顶层条目。

25个新增DOI各出现一次，缺失数和重复数均为0；题名、正式出版年、期刊、DOI、条目类型和作者数量与导入清单之间未发现不一致。排序索引包含37个排名标题和37组学习点，每篇固定3个学习点。原有12篇没有重复导入。

Rank 27继续复用写入前的既有条目，其历史条目类型仍为`webpage`；本轮没有通过数据库直写改变条目类型，以避免产生重复或绕过正常写入流程。

## 当前性边界

2026-07-18公开材料复核时，Zotero Desktop API和connector均未运行，因此本文件只能确认2026-07-14保存证据的内部一致性，不能把172条表述为2026-07-18的实时状态。网页端如需核对当前Zotero状态，必须重新连接桌面端并执行只读检查。
